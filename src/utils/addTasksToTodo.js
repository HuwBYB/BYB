export async function addTasksToTodo(userId, dailyActions, weeklyActions) {
  console.log("Adding daily/weekly actions to todos:", dailyActions, weeklyActions);
  // Loop through daily actions and add to to-do list with repeat rule
  // Loop through weekly actions similarly
}
